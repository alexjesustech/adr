name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  estrutura:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: CI estrutural
        run: bash scripts/ci-local.sh
